Class Bar {
    method Fraction foo(int y) {
        var int temp, int variable; // a variable
        let temp = (xxx+12)*-63; 
        let variable = true & false;
        let word = "hello world" < "l" > "K";
    }
}